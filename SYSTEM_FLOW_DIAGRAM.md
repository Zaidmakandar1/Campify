# Event Completion & Feedback System - Flow Diagrams

## 1. Complete System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     EVENT LIFECYCLE                             │
└─────────────────────────────────────────────────────────────────┘

Event Created → Students Register → Event Happens → Event Completed
                                                           ↓
                                    ┌──────────────────────┴──────────────────────┐
                                    │                                             │
                            Trigger Fires                                  Database Updated
                                    │                                             │
                            ┌───────┴────────┐                                   │
                            │                │                                   │
                    Notifications Created    │                                   │
                            │                │                                   │
                    ┌───────┴────────┐      │                                   │
                    │                │      │                                   │
            Student 1 Notified  Student 2 Notified                             │
                    │                │      │                                   │
                    └────────┬───────┘      │                                   │
                             │              │                                   │
                    User Sees Notification  │                                   │
                             │              │                                   │
                    ┌────────┴────────┐     │                                   │
                    │                 │     │                                   │
            Clicks Notification   Goes to Profile                               │
                    │                 │     │                                   │
                    └────────┬────────┘     │                                   │
                             │              │                                   │
                    Event Detail Page       │                                   │
                             │              │                                   │
                    Clicks "Give Feedback"  │                                   │
                             │              │                                   │
                    Fills Out Form          │                                   │
                             │              │                                   │
                    Submits Feedback ───────┴───────────────────────────────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
            Feedback Saved    Club Owner Notified
                    │                 │
            Profile Updated   Dashboard Updated
```

## 2. Notification Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    NOTIFICATION SYSTEM                          │
└─────────────────────────────────────────────────────────────────┘

Event Marked Complete
        ↓
Database Trigger: notify_event_completion()
        ↓
Query: SELECT user_id FROM event_registrations WHERE event_id = ?
        ↓
For Each Registered User:
        ↓
INSERT INTO notifications (
    user_id,
    title: "Event Completed! 🎉",
    message: "The event '[Name]' has been completed...",
    type: "event",
    related_id: event_id
)
        ↓
Supabase Real-time Subscription
        ↓
Frontend Receives Notification
        ↓
┌───────────────────────────────────┐
│  Notification Bell Updates        │
│  Badge Count Increases            │
│  Toast Notification Appears       │
└───────────────────────────────────┘
```

## 3. User Profile Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    PROFILE PAGE FLOW                            │
└─────────────────────────────────────────────────────────────────┘

User Opens Profile
        ↓
┌───────────────────────────────────────────────────────────────┐
│  Fetch Data:                                                  │
│  1. fetchProfile() - User details                            │
│  2. fetchMyFeedback() - Voice complaints                     │
│  3. fetchMyEventFeedback() - Event feedback submitted        │
│  4. fetchMyEvents() - Upcoming events                        │
│  5. fetchCompletedEvents() - Completed events ⭐ NEW         │
└───────────────────────────────────────────────────────────────┘
        ↓
Display Tabs:
        ↓
┌─────────────────────────────────────────────────────────────────┐
│  [Voice] [Upcoming] [Completed] [My Feedback]                   │
│                                                                 │
│  Completed Tab Shows:                                           │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ For Each Completed Event:                                 │ │
│  │   - Event Title & Description                             │ │
│  │   - Completion Date                                       │ │
│  │   - Check if feedback exists:                             │ │
│  │     ✓ Yes → Show "Feedback Given" badge                  │ │
│  │     ✗ No  → Show "Give Feedback" button                  │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 4. Feedback Submission Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                 FEEDBACK SUBMISSION FLOW                        │
└─────────────────────────────────────────────────────────────────┘

User Clicks "Give Feedback"
        ↓
Navigate to: /hub/event/{id}?feedback=true
        ↓
Page Loads
        ↓
useEffect Detects ?feedback=true
        ↓
Auto-scroll to Feedback Section (smooth scroll)
        ↓
User Sees Feedback Form:
┌───────────────────────────────────────────────────────────────┐
│  Overall Rating: [1] [2] [3] [4] [5]                         │
│  Organization: [1] [2] [3] [4] [5]                           │
│  Usefulness: [1] [2] [3] [4] [5]                             │
│  Would Attend Again: [Yes] [No]                              │
│  Comments: [Text Area]                                        │
│  [Submit Feedback]                                            │
└───────────────────────────────────────────────────────────────┘
        ↓
User Fills Form & Clicks Submit
        ↓
Frontend Validation
        ↓
INSERT INTO event_feedback (
    event_id,
    user_id,
    rating,
    organization_rating,
    usefulness_rating,
    would_attend_again,
    feedback_text
)
        ↓
Database Trigger: notify_event_feedback_submitted()
        ↓
Notify Club Owner
        ↓
Success Toast: "Feedback submitted!"
        ↓
Profile Updates:
  - "Feedback Given" badge appears
  - Feedback appears in "My Feedback" tab
```

## 5. Database Trigger Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE TRIGGERS                            │
└─────────────────────────────────────────────────────────────────┘

TRIGGER 1: Event Completion
───────────────────────────
UPDATE events SET is_completed = true
        ↓
AFTER UPDATE trigger fires
        ↓
IF OLD.is_completed = false AND NEW.is_completed = true
        ↓
FOR EACH user_id IN event_registrations
        ↓
INSERT notification
        ↓
RETURN NEW


TRIGGER 2: Feedback Submission
───────────────────────────────
INSERT INTO event_feedback
        ↓
AFTER INSERT trigger fires
        ↓
SELECT club owner from events JOIN clubs
        ↓
INSERT notification for club owner
        ↓
RETURN NEW
```

## 6. Data Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                    DATA MODEL                                   │
└─────────────────────────────────────────────────────────────────┘

auth.users (Supabase Auth)
    │
    ├─── profiles (1:1)
    │       │
    │       └─── clubs (1:1 for club reps)
    │               │
    │               └─── events (1:many)
    │                       │
    │                       ├─── event_registrations (1:many)
    │                       │       │
    │                       │       └─── team_members (1:many)
    │                       │
    │                       └─── event_feedback (1:many) ⭐ NEW
    │                               │
    │                               └─── Unique(event_id, user_id)
    │
    └─── notifications (1:many)
            │
            └─── Types: 'event', 'feedback_new', 'feedback_status'
```

## 7. Security Flow (RLS)

```
┌─────────────────────────────────────────────────────────────────┐
│                    ROW LEVEL SECURITY                           │
└─────────────────────────────────────────────────────────────────┘

User Requests Data
        ↓
Supabase Checks auth.uid()
        ↓
┌───────────────────────────────────────────────────────────────┐
│  event_feedback Policies:                                     │
│                                                               │
│  SELECT: Anyone can view (for aggregated stats)              │
│  INSERT: auth.uid() = user_id                                │
│  UPDATE: auth.uid() = user_id                                │
│  DELETE: auth.uid() = user_id                                │
│                                                               │
│  notifications Policies:                                      │
│                                                               │
│  SELECT: auth.uid() = user_id                                │
│  UPDATE: auth.uid() = user_id                                │
│  INSERT: System (SECURITY DEFINER)                           │
│  DELETE: auth.uid() = user_id                                │
└───────────────────────────────────────────────────────────────┘
        ↓
Return Filtered Results
```

## 8. Frontend Component Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    COMPONENT HIERARCHY                          │
└─────────────────────────────────────────────────────────────────┘

App
 │
 ├─── Navbar
 │     │
 │     └─── NotificationBell ⭐ UPDATED
 │           │
 │           ├─── Fetches notifications
 │           ├─── Subscribes to real-time updates
 │           └─── Links to event pages
 │
 ├─── Profile ⭐ UPDATED
 │     │
 │     ├─── Tabs Component
 │     │     │
 │     │     ├─── Voice Tab
 │     │     ├─── Upcoming Tab
 │     │     ├─── Completed Tab ⭐ NEW
 │     │     │     │
 │     │     │     └─── Maps completedEvents
 │     │     │           │
 │     │     │           └─── Shows "Give Feedback" button
 │     │     │
 │     │     └─── My Feedback Tab ⭐ NEW
 │     │           │
 │     │           └─── Maps myEventFeedback
 │     │
 │     └─── useEffect hooks
 │           │
 │           ├─── fetchProfile()
 │           ├─── fetchMyFeedback()
 │           ├─── fetchMyEventFeedback()
 │           ├─── fetchMyEvents()
 │           └─── fetchCompletedEvents() ⭐ NEW
 │
 └─── EventDetail ⭐ UPDATED
       │
       ├─── useSearchParams() ⭐ NEW
       ├─── feedbackSectionRef ⭐ NEW
       │
       ├─── useEffect for auto-scroll ⭐ NEW
       │
       └─── Feedback Form
             │
             └─── Comprehensive rating system
```

## 9. State Management Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    STATE FLOW                                   │
└─────────────────────────────────────────────────────────────────┘

Profile Component State:
┌───────────────────────────────────────────────────────────────┐
│  const [profile, setProfile] = useState(null)                 │
│  const [myFeedback, setMyFeedback] = useState([])            │
│  const [myEventFeedback, setMyEventFeedback] = useState([])  │
│  const [myEvents, setMyEvents] = useState([])                │
│  const [completedEvents, setCompletedEvents] = useState([])  │ ⭐
└───────────────────────────────────────────────────────────────┘
        ↓
On Mount: useEffect(() => { ... }, [user])
        ↓
Fetch All Data in Parallel
        ↓
Update State
        ↓
Re-render with New Data
        ↓
Display in Tabs
```

## 10. Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    ERROR HANDLING                               │
└─────────────────────────────────────────────────────────────────┘

Database Operation
        ↓
Try-Catch Block
        ↓
┌───────────────────────────────────────────────────────────────┐
│  Success:                                                     │
│    - Update state                                             │
│    - Show success toast                                       │
│    - Refresh data                                             │
│                                                               │
│  Error:                                                       │
│    - Log to console                                           │
│    - Show error toast                                         │
│    - RAISE WARNING in database (logged)                      │
│    - Return gracefully (don't break app)                     │
└───────────────────────────────────────────────────────────────┘
```

## 11. Real-time Subscription Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    REAL-TIME UPDATES                            │
└─────────────────────────────────────────────────────────────────┘

Component Mounts
        ↓
subscribeToNotifications()
        ↓
supabase.channel('notifications')
        ↓
.on('postgres_changes', {
    event: 'INSERT',
    table: 'notifications',
    filter: `user_id=eq.${user.id}`
})
        ↓
New Notification Inserted in Database
        ↓
Supabase Broadcasts Change
        ↓
Frontend Receives Event
        ↓
┌───────────────────────────────────────────────────────────────┐
│  1. Add to notifications array                                │
│  2. Increment unread count                                    │
│  3. Show toast notification                                   │
│  4. Update notification bell badge                            │
└───────────────────────────────────────────────────────────────┘
        ↓
Component Unmounts
        ↓
channel.unsubscribe()
```

---

## Quick Reference: Key Functions

### Database Functions
- `notify_event_completion()` - Sends notifications when event completes
- `notify_event_feedback_submitted()` - Notifies club owner of new feedback

### Frontend Functions
- `fetchCompletedEvents()` - Gets completed events for user
- `fetchMyEventFeedback()` - Gets all feedback submitted by user
- `subscribeToNotifications()` - Real-time notification updates
- `getNotificationLink()` - Routes notifications to correct pages

### Triggers
- `trigger_notify_event_completion` - Fires on event completion
- `trigger_notify_event_feedback_submitted` - Fires on feedback submission

---

This visual guide shows how all the pieces work together! 🎯
